import type { TAppConfiguration } from '~/models/types/TAppConfiguration';

const clientAConfig: TAppConfiguration = {
    apiBase: 'https://api.clientA.com',
    theme: 'dark',
    featureX: true,
    featureY: true,
};

export default clientAConfig;